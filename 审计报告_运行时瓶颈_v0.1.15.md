# kimix 运行时热点审计报告（v0.1.15）

> 审计日期：2026-07-31 | 范围：7 大运行时热点区域 | 方法：源码静态分析 + 路径追踪

---

## 审计摘要

| 优先级 | 数量 | 核心问题 |
|:------:|:----:|----------|
| **P0** | 4 | SSE 每 token 双解析、bash `read_to_end` 全量缓冲、流式终端每 tick clone、git status 无缓存调用 |
| **P1** | 3 | 每次 bash 调用 tree-sitter 命令解析、grep 全量行收集、重试预算过长 |
| **P2** | 2 | 配置文件重复加载、`serde_json::to_value` 在事件流中的额外序列化 |

---

## P0：运行时严重瓶颈

### P0-1 SSE 事件流每 token `serde_json::from_str` 双解析

**位置：** `crates/codegen/kimix-sampler/src/client.rs:69-89, 130, 983`

**证据：**
```rust
// 每个 Responses API SSE chunk 都经过两次 serde_json::from_str：
// 第一次：尝试类型化反序列化 (L70)
let mut event = match serde_json::from_str::<rs::ResponseStreamEvent>(data) {
    Ok(event) => event,
    Err(first_err) => {
        // 第二次：失败时回退到 Value 解析后重试 (L74)
        if let Ok(mut value) = serde_json::from_str::<serde_json::Value>(data) {
            // ← 每个失败 chunk 触发额外一次完整 JSON 解析
            ...
        }
    }
};

// 每个 Chat Completions SSE chunk 也做一次解析 (L983)
serde_json::from_str::<ChatCompletionChunk>(data).map_err(|e| { ... });
```

**影响：** 在 50 token/s 的流式响应中，每秒触发 50-100 次 JSON 解析。Responses API 的终端事件（`response.completed`）包含完整的 `response.usage` 嵌套结构，`apply_terminal_event_overrides`（L123-151）对相同的 `data` 再做一次 `serde_json::from_str::<Value>`，**造成同一 SSE 帧被解析三次**：类型化解析 → 失败回退 Value → 终端事件覆写 Value。

**触发频率：** 每个流式 token 一次（每秒 30-100 次）。

**典型耗时估算：** `ChatCompletionChunk` 反序列化约 5-15us/次，Responses API 终端事件三次解析可累积到约 50us。在 100 token/s 高速流中，每秒 JSON 解析占用约 0.5-2ms CPU 时间。看起来不多，但加上 tracing 日志（`tracing::info!` 在 L972-977 记录完整 `data` 字段）的开销，在 debug 环境下可达 5-10ms/s。

**修复建议：**
1. 在 `client.rs` 的 SSE 解码路径，先做 `serde_json::from_str::<Value>`，再 `from_value::<T>` 分发，失败时复用已解析的 Value
2. `apply_terminal_event_overrides` 接收已解析的 Value 引用，而非重新解析 raw data
3. L972-977 的 `tracing::info!` 记录完整 SSE data 在 debug 级别可能产生显著 I/O 开销，建议降为 `trace!`

---

### P0-2 Bash `LocalTerminalRunner::read_stream` 全量缓冲 + `read_to_end`

**位置：** `crates/codegen/kimix-shell/src/terminal/local_terminal.rs:14-18, 88-89`

**证据：**
```rust
async fn read_stream(mut stream: impl AsyncReadExt + Unpin) -> Vec<u8> {
    let mut buffer = Vec::new();
    let _ = stream.read_to_end(&mut buffer).await;  // ← 无上限全量缓冲
    buffer
}

// 调用处：
let stdout_task = tokio::spawn(read_stream(stdout));  // L88
let stderr_task = tokio::spawn(read_stream(stderr));  // L89
```

**影响：** `read_to_end` 在子进程结束前阻塞整个任务。对于编译输出、大文件 cat、日志 dump，stdout 可达数 MB 甚至数十 MB。分配 10MB 缓冲区 + 内存拷贝的耗时约为 2-5ms（取决于分配器），且占用等量内存直到请求结束。关键问题是这发生在 `LocalTerminalRunner` 中——虽然生产环境主要用 `StreamingLocalTerminalRunner`，但两个终端后端并存，调用者可能根据配置路由到任一实现。

**触发频率：** 每个 bash 工具调用一次。

**典型耗时估算：** 10MB 输出约 2-5ms（内存分配 + 拷贝），100MB 输出约 30-50ms + 可能触发 OOM。

**修复建议：**
1. `LocalTerminalRunner` 统一迁移到 `StreamingLocalTerminalRunner` 的流式模式，或至少在 `read_stream` 中加 `output_byte_limit` 截断
2. 在 `truncate_buffer`（L25-45）的同等逻辑前移到 `read_stream` 读取循环中

---

### P0-3 流式终端每 100ms tick 全量 clone 输出缓冲区

**位置：** `crates/codegen/kimix-shell/src/terminal/streaming_local_terminal.rs:679-691`

**证据：**
```rust
// L19-20: 默认通知间隔 100ms
const DEFAULT_NOTIFICATION_INTERVAL_MS: u64 = 100;
const READ_BUFFER_SIZE: usize = 8192;

// L679-691: 每次 tick 都 clone 整个 output_buf
_ = ticker.tick() => {
    if truncate_buffer(&mut output_buf, request.output_byte_limit) {
        truncated = true;
        last_sent_len = 0;
    }
    {
        let mut state = output_state.lock().await;
        state.output = output_buf.clone();  // ← 全量 clone
        state.truncated = truncated;
    }
    if output_buf.len() > last_sent_len {
        self.send_update(...);  // L698
    }
}
```

**影响：** 默认 `output_byte_limit` 可达数 MB。如果命令产生 5MB 输出，每 100ms 做一次 5MB 的 `clone()`（内存分配 + 拷贝），持续占用 CPU。对于 30 秒的长时间命令，累积 clone 次数可达 300 次。

**触发频率：** 每个前台 bash 命令期间，每 100ms 一次。

**典型耗时估算：** 5MB clone 约 1-2ms，100ms 周期内会积压。如果 `send_update`（L698）中的 ACP 通知发送因为网络延迟阻塞超过 100ms，下一次 tick 可能和本次重叠，进一步放大。

**修复建议：**
1. 改为只保存增量（`output_buf[last_sent_len..]`）到 state 中，而非全量 clone
2. 或者改用 `Arc<String>` / `Bytes` 共享只读视图
3. 考虑将 tick 间隔和缓冲区大小挂钩：小缓冲区高频 tick，大缓冲区低频 tick

---

### P0-4 git status 每次提示词渲染都走 `spawn_blocking` + CLI 子进程

**位置：** `crates/codegen/kimix-workspace/src/file_system/git_status.rs:18-24, 48-50`
和 `crates/codegen/kimix-workspace/src/session/git.rs:59`

**证据：**
```rust
// git_status.rs:21 - 每次调用都 spawn_blocking + 创建子进程
pub async fn git_status(working_directory: impl Into<PathBuf>) -> Result<String, FsError> {
    tokio::task::spawn_blocking(move || git_status_impl(&working_directory))
        .await
        .map_err(|e| FsError::Other(format!("git status task failed: {}", e)))?  // L21-23
}

// git.rs:59 - 缓存 TTL 仅 2 秒
pub const GIT_STATUS_CACHE_TTL: Duration = Duration::from_secs(2);
```

**影响：** `git status --short --branch` 作为子进程运行，包含 fork/exec 开销 + git 自身索引扫描。在大仓库（10 万+文件）中单次调用可达 200-500ms。`spawn_blocking` 将阻塞操作移到 tokio 阻塞线程池，是正确的——但频繁调用仍占用线程池资源。2 秒 TTL 缓存存在但在实际使用路径（`workspace_ops.rs:248` 调用 `crate::file_system::git_status(cwd)`）中似乎未使用缓存版本。

**触发频率：** 每次系统提示词渲染时（每轮对话一次），加上手动 `git status` 工具调用。

**典型耗时估算：** 大型仓库 200-500ms，中型仓库 20-50ms。

**修复建议：**
1. 确保系统提示词渲染路径使用 `crate::session::git::status`（带缓存的路径），而非 `crate::file_system::git_status`（无缓存）
2. 提高默认 `GIT_STATUS_CACHE_TTL` 到 5-10 秒，用 fsnotify 事件主动失效
3. 考虑用 `gix` 库替代 `git` CLI 子进程，避免 fork/exec 开销（`kimix-gix-status` crate 已存在但似乎未用于 `git_status_short`）

---

## P1：中度运行时瓶颈

### P1-1 每次 bash 工具调用都执行 tree-sitter 命令分解

**位置：** `crates/codegen/kimix-workspace/src/permission/manager.rs:366-383`

**证据：**
```rust
pub(crate) fn evaluate_bash_segments(cmd: &str, state: &PermissionState) -> SegmentEvaluation {
    evaluate_bash_segments_inner(cmd, state, true)  // 每次都解析
}

fn evaluate_bash_segments_inner(...) -> SegmentEvaluation {
    let Some(segments) = all_commands_from_script(cmd) else {  // ← tree-sitter 解析
        return SegmentEvaluation::Unparseable;
    };
    // 遍历所有 segment 做权限检查...
}
```

`all_commands_from_script` 调用 tree-sitter bash 语法解析器（`bash_command_splitting.rs`），解析命令字符串为 AST 并提取链式命令段。这包括：
- 引用解析（单引号、双引号）
- heredoc 解析
- 管道 `|`、逻辑运算符 `&&` `||`、分隔符 `;` 分割
- 嵌套命令替换 `$()` 和反引号

**影响：** tree-sitter 解析一个中等长度的 bash 命令（如含 heredoc 的多行脚本）可能耗时 0.5-2ms。普通单行命令约 50-200us。每次 bash 工具调用都触发。

**触发频率：** 每个 bash 工具调用一次。在自动化工作流中可达每秒 2-5 次。

**典型耗时估算：** 单行命令 50-200us，多行脚本 0.5-2ms。

**修复建议：**
1. 对相同命令字符串做 LRU 缓存（命令文本频繁重复）
2. 简单模式快速路径：对于不含特殊字符（`|&;$()`\'\"`\`）的简单命令，跳过 tree-sitter 解析，直接做空格分词后匹配

---

### P1-2 grep 结果全量行收集 + 双重格式化

**位置：** `crates/codegen/kimix-tools/src/implementations/kimix/grep/mod.rs:1067-1099`

**证据：**
```rust
// L1068: 全量收集所有行
let mut output_lines: Vec<String> = stdout.lines().map(|s| s.to_string()).collect();

// L1075-1076: 然后 parse 为 file_matches
let file_matches = if matches!(config.output_mode, OutputMode::Content) {
    parse_file_matches(&output_lines, config.max_chars_per_line)
} else {
    Vec::new()
};

// L1081-1095: 再遍历一遍做 match counting
let match_count_value = match config.output_mode {
    OutputMode::Content => count_matches(&output_lines),
    ...
};
```

**影响：** 在大仓库 grep 产生数万行结果时，`stdout.lines().map(|s| s.to_string()).collect()` 分配数万个 String。然后 `parse_file_matches` 再对每行做正则匹配和截断。`count_matches` 又遍历一遍。三遍遍历 + 全量分配。

**触发频率：** 每个 grep 工具调用一次。

**典型耗时估算：** 1 万行结果约 2-5ms（分配 + 三遍遍历），10 万行约 20-50ms。

**修复建议：**
1. 合并三遍遍历为一遍（边读行边累加计数 + 边构建 file_matches）
2. 在 `read_rg_stdout_capped` 中提前终止（`head_limit` 已存在但后续仍对所有行做 parse_file_matches）

---

### P1-3 深重试预算放大故障恢复时间

**位置：** `crates/codegen/kimix-sampler/src/retry.rs:47-63`

**证据：**
```rust
pub const RATE_LIMIT_RETRY_THRESHOLD: u32 = 2;
pub const STREAM_TRANSPORT_RETRY_THRESHOLD: u32 = 3;
pub const DEFAULT_MAX_RETRIES: u32 = 15;  // ← 最高 15 次重试

// L96-100: 指数退避到 30s 封顶
pub fn retry_backoff_with_jitter(retry_count: u32) -> Duration {
    let shift = retry_count.saturating_sub(1);
    let base_ms = 2000u64.checked_shl(shift).unwrap_or(u64::MAX).min(30_000);
    // 2s → 4s → 8s → 16s → 30s → ... → 30s
}
```

**影响：** 在 15 次重试下，前 4 次退避约 30 秒（2+4+8+16≈30），后 11 次每 30 秒 ≈ 5.5 分钟，总重试预算约 6 分钟。中间流中断（`STREAM_TRANSPORT_RETRY_THRESHOLD = 3`）已有较低的上限，但非流式的 500/502/503 错误会走完整退避。虽然 `STREAM_TRANSPORT_RETRY_THRESHOLD = 3` 限制了流中断重试次数是好的设计，但在连续 5 次 503 后继续等待 30 秒乘 10 次 = 5 分钟，用户体验差。

**触发频率：** 仅在 API 错误时触发，正常情况不触发。

**修复建议：**
1. 非流式路径也区分重试类型：对 503（临时过载）保留长退避，对 502（网关错误）降到 5 次封顶
2. 总重试时间封顶：无论重试次数多少，总时间不超过 2 分钟

---

## P2：低优先级瓶颈

### P2-1 配置文件重复 load 未充分利用热重载

**位置：** `crates/codegen/kimix-shell/src/agent/app.rs:679-687`

**证据：**
```rust
// L680: 在 startup 中 load 一次
let user_cfg = crate::config::load_from_disk().ok();

// L681: 又 load 一次 requirements
let requirements = crate::agent::config::read_requirements_toml();

// L682-686: resolve 合并
let recursive_config_watch_enabled = {
    ...
    crate::util::config::resolve_mcp_recursive_config_watch(
        requirements.as_ref(),
        user_cfg.as_ref(),
        /* managed */ None,
    )
};
```

**影响：** `load_from_disk` 在启动时多次调用（agent/config.rs:197 也调用 `load_effective_config`）。每次调用读取并解析磁盘 TOML 文件。虽然在启动路径上（非热路径），但对于快速启动优化来说可避免。

**触发频率：** 启动时一次。

**修复建议：** 缓存首次 load 结果，后续调用复用。

---

### P2-2 `serde_json::to_value` 在事件流中的额外序列化

**位置：** `crates/codegen/kimix-sampler/src/stream/responses.rs:353, 367`
和 `client.rs:798, 855`

**证据：**
```rust
// responses.rs:353, 367 - 对后端工具调用结果做 to_value 再嵌入事件
let result = serde_json::to_value(ws).ok();
yield SamplingEvent::BackendToolCallCompleted { result, ... };

// client.rs:798, 855 - 对已经序列化过的 payload 再做 to_value + JSON 操作
let mut request_body = serde_json::to_value(&payload).map_err(|e| { ... })?;
crate::kimi_compat::adapt_chat_completions_body(&mut request_body, ...);
```

**影响：** 每次 API 请求将 `ChatCompletionRequest` 序列化为 `serde_json::Value`（全量 walk），然后通过 `.json(&request_body)` 再次序列化为字节流。实际上是「结构体 → Value → 字节」的双重序列化。

**触发频率：** `client.rs:798` 每个 chat completion 请求一次；`client.rs:855` 每个流式请求一次；`responses.rs` 仅在后端工具调用完成时触发（低频）。

**典型耗时估算：** 大型 ChatCompletionRequest（含 tools、messages）序列化到 Value 约 100-500us，随后 reqwest 再次序列化 Value → 字节也约 100-500us。

**修复建议：** `ChatCompletionRequest` 的 Kimi 兼容适配改为在结构体级别操作，而非先序列化到 Value 再修改。`StreamingChatRequest` 已有的 `#[serde(flatten)]` 模式是正确的方向。

---

## 特别表扬

### 良好实践

1. **FsNotify 共享 watcher**（`kimix-fsnotify/src/source.rs:103-140`）：多 session/subagent 复用同一目录的 OS watcher，`Weak` 引用自动清理，避免重复创建 inotify/FSEvents 句柄。设计合理。

2. **gix 线程预算控制**（`kimix-gix-status/src/lib.rs:10-35`）：显式限制 `gix` 的 produce worker 数（hard cap 8），考虑 nproc 约束，防止在 `panic=abort` + 低 RLIMIT_NPROC 环境下 crash。

3. **终端退出超时边界**（`streaming_local_terminal.rs:38`）：`KILL_REAP_TIMEOUT = 2s` 防止 wedged 子进程阻塞 kill 路径，配合 `KillOnDrop` 双保险。

4. **git CLI `--no-optional-locks`**（`session/git.rs:67-75`）：所有 git 命令统一加 `--no-optional-locks`，避免后台 stat-cache 刷新创建 `index.lock` 文件。

5. **SSE 解码的 BOM 处理**（`client.rs:938-949`）：正确处理 UTF-8 BOM 的字节偏移 bug 变通方案。

---

## 最关键 3 个运行时瓶颈（300 字总结）

**第一：SSE 每 token JSON 双解析 + 日志 I/O。** 每个 Chat Completions 流式 chunk 做一次 `serde_json::from_str`，Responses API 在极端情况下对同一帧做三次 JSON 解析（类型化 → Value 回退 → 终端覆写）。每秒 50 token 意味着每秒 50-150 次解析，叠加 L972-977 的 `tracing::info!` 完整 SSE data 日志，在 debug 级别产生秒级数毫秒的纯 CPU 开销。修复：复用首次 Value 解析结果、降日志级别为 trace。

**第二：bash 输出全量 clone + buffer 膨胀。** StreamingLocalTerminal 每 100ms 将整个输出缓冲 clone 到共享状态（`output_buf.clone()`）。5MB 输出对应 300 次 5MB clone = 1.5GB 内存搬运。这是持续 CPU 占用的主要来源，不是一次性峰值。修复：只传增量或改用共享引用类型。

**第三：git status 的 fork/exec 开销叠加重试放大。** 大型仓库的 `git status --short --branch` 子进程耗时 200-500ms，在系统提示词渲染中被同步等待。2 秒 TTL 缓存存在但未在所有路径使用。修复：统一使用缓存路径、用 fsnotify 事件主动失效、渐进迁移到 gix 库内实现。
