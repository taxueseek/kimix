{
  "name": "add-function",
  "files": {
    "utils.py": "def double(x):\n    return x * 2\n"
  },
  "prompt": "Add a function `triple(x)` to utils.py that returns x * 3. Keep the existing double() function unchanged.",
  "assert": [
    { "type": "file_contains", "path": "utils.py", "pattern": "def triple" },
    { "type": "file_contains", "path": "utils.py", "pattern": "def double" },
    { "type": "command_succeeds", "cmd": "python3 -c \"from utils import double, triple; assert double(2)==4 and triple(2)==6\"" }
  ],
  "max_turns": 6,
  "timeout_sec": 180
}
