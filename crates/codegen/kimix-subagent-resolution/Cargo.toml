[package]
license = "Apache-2.0"
name = "kimix-subagent-resolution"
version.workspace = true
edition.workspace = true
description = "Subagent configuration resolution: merges persona, role, and spawn-time overrides into a resolved spec"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
kimix-sampling-types = { path = "../kimix-sampling-types" }
kimix-tools = { path = "../kimix-tools" }
kimix-tool-types.workspace = true
# TODO(phase2): add these when resolve_subagent_spec() composition is implemented:
# kimix-agent = { path = "../kimix-agent" }  # AgentDefinition lookup
# kimix-fast-worktree = { path = "../kimix-fast-worktree" }  # worktree creation

[dev-dependencies]
tempfile = { workspace = true }
toml = { workspace = true }

[lints]
workspace = true
