[package]
license = "Apache-2.0"
name = "kimix-shell-base"
version.workspace = true
edition.workspace = true
description = "Foundation modules for the kimix shell crate family: environment presets, CPU profiling, and process/filesystem utilities."

[features]
# CI default set: builds a single shared rlib per crate, so downstream test
# targets need the test-only helper surface compiled in.
default-bazel = []

[dependencies]
agent-client-protocol = { workspace = true }
anyhow = { workspace = true }
chrono = { workspace = true }
reqwest = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true, features = ["sync"] }
tracing = { workspace = true }
url = { workspace = true }
kimix-config = { workspace = true }
kimix-env = { workspace = true }
kimix-shared = { workspace = true }
kimix-version = { workspace = true }
kimix-token-estimation = { workspace = true }
kimix-tty-utils = { workspace = true }

[target.'cfg(unix)'.dependencies]
libc = { workspace = true }
nix = { workspace = true }
# No `flamegraph` feature: that pulls in inferno (CDDL-1.0), which we keep out
# of shipped binaries. stop() emits folded stacks via pprof's public Report
# API instead; render externally with speedscope or inferno-flamegraph.
pprof = { workspace = true }

[target.'cfg(windows)'.dependencies]
windows = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
# `cfg(test)` in this crate does not enable features on dependencies, so the
# tests' `EnvVarGuard` re-export needs the feature turned on explicitly.
kimix-env = { workspace = true, features = [] }

[lints]
workspace = true
