//! API-agnostic conversation representation.
//!

//! The canonical types now live in `kimix_sampling_types::conversation`.
//! This module re-exports them.

// Re-export everything from the standalone crate.
pub use kimix_sampling_types::conversation::*;

// Tests for conversation types now live in Kimix-sampling-types crate.
