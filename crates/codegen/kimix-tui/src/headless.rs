//! Headless mode — re-exported from the independent `kimix-headless` crate.
//!
//! All types and functions live in `kimix_headless`; this module keeps
//! existing `crate::headless::*` paths working throughout the pager.
pub use kimix_headless::*;
